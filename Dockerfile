# Используем официальный образ Golang
FROM golang:1.23

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файлы зависимостей и загружаем их отдельно (для кэширования)
COPY go.mod go.sum ./
RUN go mod download

# Копируем всё остальное
COPY . .

# Собираем бинарник
RUN go build -o app

# Задаём команду по умолчанию для запуска
CMD ["./app"]
